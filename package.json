{
  "name": "shifts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"pnpm -F backend dev\" \"pnpm -F frontend dev\"",
    "build": "pnpm -F backend build && pnpm -F frontend build",
    "test": "pnpm -r test",
    "db:migrate": "pnpm -F backend db:migrate",
    "db:seed": "pnpm -F backend db:seed",
    "db:reset": "pnpm -F backend db:reset"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
